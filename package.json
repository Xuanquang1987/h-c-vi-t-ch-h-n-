{
  "name": "trang-tap-viet-tieng-trung",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android:sync": "npm run build && npx cap sync android && node scripts/patch-capacitor-settings.mjs",
    "patch:cap": "node scripts/patch-capacitor-settings.mjs"
  },
  "devDependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/cli": "^7.2.0",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@capacitor/core": "^7.2.0",
    "hanzi-writer": "^3.7.3"
  }
}
